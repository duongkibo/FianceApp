<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.qlct.mymoney">

    <application
        android:allowBackup="true"
        android:icon="@drawable/img_splash_4"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bills"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".CreateUserActivity" />
        <activity android:name=".PinPasswordActivity" />
        <activity android:name=".authenticate.LoginActivity"/>
        <activity android:name=".ui.AddGroupActivity" />
        <activity
            android:name=".ViewsSildeActivity"
            android:theme="@style/AppTheme.FullScreen" />

        <activity android:name=".MainActivity"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".adapter.NotificationReceiver" />
    </application>

</manifest>